{
  "name": "dokku-simple-python-vue-spa",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm ci --prefix frontend --production false && npm --prefix frontend run docs:build && npm install --ci frontend --production true"
  },
  "engines": {
    "node": "16.x"
  }
}
