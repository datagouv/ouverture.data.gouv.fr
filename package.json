{
  "name": "dokku-simple-python-vue-spa",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm ci --prefix frontend && npm --prefix frontend run docs:build && npm ci --prefix frontend --omit=dev"
  },
  "engines": {
    "node": "16.x"
  }
}
